# AI Provider Configuration
# Supported: claude-sonnet-4-5-20250929, claude-opus-4-6, gpt-4, gpt-4-turbo, etc.
LLM_MODEL=claude-sonnet-4-5-20250929

# API Keys (set the one matching your model)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Docker tools container name
TOOLS_CONTAINER=shadowpulse-tools

# Database
# For docker compose (backend container connects to Postgres service name "postgres"):
DATABASE_URL=postgresql+asyncpg://shadowpulse:shadowpulse@postgres:5432/shadowpulse

# For local dev backend (running outside Docker), use:
# DATABASE_URL=postgresql+asyncpg://shadowpulse:shadowpulse@localhost:5432/shadowpulse
